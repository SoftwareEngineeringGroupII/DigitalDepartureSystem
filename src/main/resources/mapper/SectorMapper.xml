<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<!--namespace=绑定一个对应的mapper接口-->
<mapper namespace="com.digitaldeparturesystem.mapper.SectorMapper">

    <select id="findOneByEmail" parameterType="java.lang.String" resultType="com.digitaldeparturesystem.pojo.Clerk">
        select * from clerk where clerkEmail = #{clerkEmail} and clerkStatus = 1;
    </select>

    <select id="findOneByClerkAccount" parameterType="java.lang.String" resultType="com.digitaldeparturesystem.pojo.Clerk">
        select * from clerk where clerkAccount = #{clerkAccount} and clerkStatus = 1;
    </select>

    <select id="findOneById" parameterType="java.lang.String" resultType="com.digitaldeparturesystem.pojo.Clerk">
        select * from clerk where clerkID = #{clerkId} and clerkStatus = 1;
    </select>

    <update id="updateClerk" parameterType="com.digitaldeparturesystem.pojo.Clerk">
        update clerk set
        clerkName = #{clerkName},
        clerkPwd = #{clerkPwd},
        department = #{department},
        clerkAccount = #{clerkAccount},
        clerkPhoto = #{clerkPhoto},
        clerkStatus = #{clerkStatus},
        clerkEmail = #{clerkEmail}
        where clerkID = #{clerkID};
    </update>
</mapper>